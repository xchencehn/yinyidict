# 本地中英词典

纯自用的离线中英词典桌面应用。完全本地运行，断网可用。
原生 Rust 单二进制，不依赖 node / python / java。

```
cargo run --release -p dict-app        # 或直接跑 target/release/dict.exe
```

---

## 目标与约束

- **完全本地。** 任何「偷偷联网」的方案直接出局 —— 浏览器 `speechSynthesis`
  里的远程声音、云 TTS 都不行。发音是本机 sherpa-onnx 现场合成。
- **查询逐字符响应，无感知延迟。** 不做 debounce：debounce 是延迟的主要来源，
  不是解药。实测所有查询 0.15–2.3 ms。
- **中文词和英文词都是一等词头**，各有独立词条页。
- **界面极简**：一个输入框，下面一块区域。

因为纯自用，以下问题不需要考虑：CC-CEDICT 的 BY-SA 传染性、ECDICT 授权不明、
Tatoeba 逐句署名 —— ShareAlike 只在再分发时触发。
唯一仍然成立的红线：商业词典的 MDX 文件（牛津/朗文/柯林斯）是受版权保护的内容，不碰。

---

## 现状

| 部件 | 状态 |
|---|---|
| 词库 | 142.5 万条词头（中文 12.5 万 + 英文 130 万），412 MB 索引 |
| 检索 | 7 通道并发 + 归并，0.15–2.3 ms |
| 义项重组 | 10,645 条高频词已重组（Claude Sonnet 5），90% 完成义项合并，源义项覆盖 99.96% |
| 界面 | 两态面板、声调着色、淡入动效、F1 调参条、独立设置窗口 |
| 常驻 | 系统托盘 + 全局热键 Alt+1 唤出词典；关窗收进托盘；可开机自启 |
| 启动 | 窗口 0.39 s 出来；语音引擎在第一帧之后才起，约 3.2 s 就绪，不挡界面 |
| 发音 | Kokoro 多语，中英同模型**同一个固定音色**，载体句合成再裁剪，**GPU RTF ≈ 0.04**（CPU 0.35）|
| 测试 | 80 个单元测试 + `tools/tray-matrix.ps1` 的窗口行为端到端 |
| 发布 | 打 `v*` 标签由 CI 出包，4 MB，数据由 `dict-build fetch` 现取 |

`shots/` 里有十二张各状态的截图，是程序自己截的（见下文「自截图」）。

## 布局

```
crates/
  dict-core/    数据模型、只读 mmap 词库、多路检索与打分、动态库加载
  dict-build/   构建期 ETL：原始数据 → mmap 索引；另含 dict-query 命令行查词
  dict-tts/     sherpa-onnx FFI + winmm 播放 + 载体句裁剪
  dict-app/     eframe/egui 界面（bin 名为 dict）
data/raw/       原始数据源（gitignore）
data/index/     生成的词库（gitignore）
models/         语音模型（gitignore）
vendor/         sherpa-onnx 预编译运行库（gitignore）
.bin/           构建工具变通用的本地副本（gitignore，用 tools/setup-toolchain.sh 重建）
```

## 从零重建

**有 MSVC build tools 的机器**（大多数 Windows 开发机）什么都不用配：

```bash
tools/fetch-data.sh                    # 一次性：约 1.2 GB 数据源和模型
cargo run --release -p dict-build      # 约 30 秒，产出 data/index
cargo run --release -p dict-app        # 跑起来
```

**没有 MSVC 的机器**（比如本机）先多一步，它会装好 windows-gnu 那套并生成
`.cargo/config.toml`：

```bash
tools/setup-toolchain.sh               # 一次性，见下文「工具链」
```

只想跑测试、不想下 1.2 GB 数据的话，`cargo test` 就够了 ——
ETL 的集成测试用的是 `crates/dict-build/tests/fixture` 里那份微型词库。

---

## 数据源

### 中文侧词头
| 用途 | 来源 | 备注 |
|---|---|---|
| 词条/拼音/释义 | CC-CEDICT | 124,968 条，MDBG 下载 |
| 词频 + 词性 | **jieba 词典** | 34.9 万词。**与原设计不同**，见下 |
| 例句 | Tatoeba cmn-eng 对照 | 6.6 万句对，挂上 4.0 万条 |
| 异体字 | Unihan | **用 kSemanticVariant 等，不是 kZVariant**，见下 |
| 注音 | CC-CEDICT 自带 | 词级标注，无需 pypinyin |

### 英文侧词头
| 用途 | 来源 | 备注 |
|---|---|---|
| 全部字段 | ECDICT (skywind3000) | 340 万行，按质量筛到 130 万 |
| 词频 | ECDICT 内置 BNC/COCA | 排名，越小越常用 |
| 例句 | Tatoeba | 挂上 3.2 万条 |

### 与原设计的三处偏差

1. **词频源：SUBTLEX-CH → jieba 词典。** SUBTLEX-CH 在 crr.ugent.be 的下载
   已失效（返回 HTML 而不是 zip）。jieba 的计数同样是大规模语料统计量，
   覆盖 34.9 万词，还顺带给了词性。运行期只比百分位，换语料不影响打分正确性。

2. **异体字字段：kZVariant → kSemanticVariant + kSimplified/TraditionalVariant。**
   `kZVariant` 在现行 Unihan 里只剩 **149 条**，已是遗留字段，
   而设计里点名要解决的「裡/裏/里」「着/著」都不在其中。改用那三个字段后
   拿到 15,410 组，`zh` 通道的键从 19.9 万涨到 32.2 万。

3. **存储：SQLite FTS5 → 自有 mmap 格式 + FST。** 读一次写多次的词库不需要
   SQL 引擎，自有格式还省掉了一个 C 依赖（这台机器编不了 C，见「工具链」）。
   ECDICT 本身是 SQLite，构建期用 Windows 自带的 `winsqlite3.dll`
   （dlopen，见 `crates/dict-build/src/sqlite.rs`）读它，全程零 C 编译。

### 注音必须词级消歧

注音一律取 CC-CEDICT 的**词级**标注，不做字级映射再拼合 ——
「银行 yin2 hang2」这类多音字，只有词级标注是对的，字级会拼出 yin2 xing2。

---

## 检索：多路并发 + 归并

每次击键**所有通道全跑再归并**。不要做 lane 检测分支 —— ASCII 输入天然歧义
（`man` 既是英文词也是 màn/mǎn 的拼音），分支会漏。只有含 CJK 字符时才能
确定地关掉英文通道。

| 通道 | 输入示例 | 匹配对象 | 权重 |
|---|---|---|---|
| `zh` | `中国` | 中文词头前缀（含繁体、异体） | 1.00 |
| `enw` | `nation` | 英文词头前缀（含屈折形→原型） | 1.00 |
| `py` | `zhonggu` | 无调拼音串（ü 兼容 `v`） | 0.95 |
| `zhsub` | `国` | 中文词头内部子串 | 0.85 |
| `ini` | `zg` | 拼音首字母串 | 0.70 |
| `gloss` | `slow` | 中文词的英文释义（二等） | 0.50 |
| `trans` | `编译` | 英文词的中文释义（二等） | 0.50 |

`zhsub` 是索引词头的全部**真后缀**，在这张表上做前缀查询即得「包含」语义。

### 打分

```
score = w_lane · SCALE · freq_pct + exact_bonus − λ·(keyLen − queryLen)
```

公式只有一份实现（`Params::score`），构建期和运行期共用 —— 短前缀预算表
选出的「前 N 名」必须和运行期排序是同一回事。

四条规则，都是踩过的坑：

1. **词频必须先百分位化。** jieba 是语料计数，BNC/COCA 是排名，量纲完全不同，
   直接进同一个打分函数会得出胡话。构建期各自转成 0..1，运行期只比百分位。
2. **exact_bonus 是词典和输入法的分野。** 输入法猜你想打什么，词典用户通常
   已经知道要查什么。完整前缀命中必须压过高频词，否则查「事」会被「时间」
   一直挡住。默认 6.0。
3. **词头命中给满额，释义命中给半额。** 否则打 `china` 会因为「中国」词频高
   而永远拿不到英文的 china（瓷器）。
4. **释义路还要封顶（默认 3 条）。** 只靠 0.5 的权重压不住：释义词元被完整
   打中时也拿 exact_bonus，一个常见义项能同时命中几十个英文词。不封顶的话
   查「编译」会被 redacted / Aimaco 这类噪音挤掉「编译器」。

### 短前缀预算表

一字符查询在英文侧要流过九万个键，fst 逐键重建键字节约需 25 ms —— 这是每次
查英文单词的第一次击键都要付的代价。构建期给候选量大的前缀预算出前 400 名，
运行期直接查表。

实测 `a` 24.5 ms → 0.81 ms，`s` 35.0 ms → 0.15 ms，代价 0.65 MB。

排名用默认参数打分；界面滑块调参后名次可能与真实排序略有出入，但 400 的深度
远超 8 条候选的需要，实际看不出来。

### 稳定排序

query 是上一次的延长时**只过滤不重排**。用户眼睛已经锁定第 3 项时它跳到第 1 项，
比慢更烦人。前缀扩展时匹配集是子集，过滤天然保序。

---

## 界面

**两态面板**：输入框下面同一块区域，要么候选、要么词条，不共存。

返回路径（进了词条页必须出得来）：
- 打字 → 退回候选
- ↑/↓ → 退回候选并**保留原选中项**（可以连着翻下一个）
- Esc 两段 → 一退候选，二退清空
- 回车 = 打开当前选中的候选，不是拿原始输入去精确查询（否则 `zhonggu` 回车查无结果）
- F1 → 开关底部调参条（默认隐藏）

**输入框的占位文字固定是 `en-zh`**，用比 `faint` 还淡一档的 `ghost` 色。
两个理由：空框里放长句子（早先是「中文 · zhongwen · zw · english」，
再早是上次查过的词）会被当成已经输进去的内容；而这个框唯一需要说明的
就是「中英都能查」。颜色也必须一眼能和真输进去的字区分开。

**词条页只留词义**：词频、考试标签（四级/六级/考研/托福/GRE、柯林斯星级）都不显示 ——
那是语料统计，不是这个词本身的信息，查词的人要的是词义不是应试来历。
词频仍然在幕后决定候选排序，只是不占版面。
**「原始释义」折叠区已从词条页去掉**（中英文都去掉）。核对能力没有丢 ——
`raw` 字段仍在词库里，`dict-query --raw <词>` 会把重组后的义项和原文并排打出来。
中文侧的义项被 LLM 重组过，怀疑哪条时一条命令就能对照，只是不再占用界面。

**词条页两种形态，靠字体和注音区分，不加标签**：
- 中文词：衬线大字 + 声调着色拼音（1 深玫瑰 / 2 琥珀 / 3 苔绿 / 4 靛蓝 / 轻声灰）
- 英文词：无衬线大字 + 灰色 IPA + 形态行（时态/复数/比较级，来自 ECDICT exchange）

颜色的有无本身就是类型信号。全局只有声调这一处用色，其余墨色和灰。
无卡片、无边框、无阴影。动效只有一处：词条出现时 110 ms 淡入上浮；
回到候选是瞬时的 —— 前进要交代「换内容了」，后退应该像撤销一样立刻发生。

字体从系统里挑，优先 `NotoSerifSC-VF` / `NotoSansSC-VF`（Windows 11 自带），
退回华文宋体 / 微软雅黑。

### 发声键

词条页有三处能出声：词头一处，每条例句的**两行各一处** ——
例句是中英对照的，查中文时想听英文那句、查英文时想听中文那句都是常事。

图标是画出来的，不是字形：**一个点 + 两道向外扩的弧**，横过来的信号图标。
早先用的是文字 `▶`，但三角形是「播放」，播放的是一段录音；
这里按下去是**现场合成**，画声波比画播放键切题。而且字形按钮的大小受字体
摆布，画出来的能压到 12.5 px 还不糊。词头那个 17 px，例句的小一圈。

键都在文字**后面**，不在前面：例句左边已经有一根竖线在做视觉分组，
再插一列图标会把两种分组信号叠在一起。用 `horizontal_wrapped` 排，
长句子换行后发声键跟在最后一行末尾，不会被挤出可视区。
垂直位置按行高算（`(字号 × 1.32 − 边长) / 2`）压下去，否则图标会顶着行首浮着。

### 标题栏是自绘的

`with_decorations(false)`，标题栏由 `App::title_bar` 自己画：左边一个淡淡的
「词典」，右边四个键 —— **钉在最前 / 最小化 / 最大化 / 关闭**。

为什么不用系统的：一是那条灰边在这套「无卡片无边框」的配色里格格不入；
二是**原生标题栏加不了按钮**，而那颗钉子必须在那一排里才符合直觉。

四个图标都是画出来的，不是字形（和发声键同一个理由）。钉子的状态**靠姿势而不是
靠颜色**：钉住时立起来、帽子填实，没钉住时歪着、帽子只描边。
一开始画成「圆点 + 一根斜线」，看起来就是个放大镜，改成「横帽子 + 竖针」才认得出。

自绘的代价是系统那套交互全丢了，得一条条接回来：

| 丢了什么 | 怎么接回来 |
|---|---|
| 拖动窗口 | 标题栏空白处 `StartDrag`（底下还是 `WM_NCLBUTTONDOWN`，贴边分屏照常有）|
| 双击最大化 | `Maximized(!maxed)` |
| 八个方向缩放 | 窗口边缘 6 px 的窄条 + `BeginResize` |

> **踩过的坑：缩放边必须在内容之后注册。** egui 的命中判定是后来者居上 ——
> 先注册的话，输入框和滚动区会把贴边那几像素的点击全吃掉，边就永远拖不动。
> 所以 `resize_edges` 是在 `ui()` 的最后一行才调的，不在 `title_bar` 里面。

窗口大小**记在 `settings.json` 里**，拖一次就记住，下次照这个开
（`App::remember_size`）。最大化时不记 —— 记了的话点「还原」会还原成全屏尺寸，
等于还原不回去。设置子窗口用同一个尺寸，两个不一样大的话来回切会跳一下。

### 常驻与设置

托盘和全局热键跑在**自己的线程**上，有自己的 message-only 窗口和消息循环 ——
想复用 eframe 的窗口就得子类化它的 WndProc，那是在跟 eframe 抢消息。
另起一个只收消息的窗口干净得多，代价只是一个线程。

托盘事件在 `App::logic()` 里处理，不是 `ui()`：**窗口藏起来时 egui 不出帧**，
`ui()` 根本不会被调用，按了热键也叫不出窗口。托盘线程发完事件还要
`ctx.request_repaint()` 把界面叫醒。

- **托盘菜单**：显示词典 / 设置 / 退出。左键单击 = 显示或隐藏。
- **全局热键 `Alt+1`** 唤出/收起词典，可改。**被占时自动顺着备选往下找**并明确
  告知 —— 全局热键被别的软件占是常事（本机实测 `Ctrl+Alt+D`、`Ctrl+Alt+空格`、
  `Win+D`、`Ctrl+Alt+F` 都已被占，`RegisterHotKey` 返回 1409），
  与其让这个功能静默死掉，不如换一个。
  注意注册全局热键等于从全系统抢走这个组合，而 Alt+数字有些输入法在用。
  设置窗口不占热键 —— 走托盘菜单进。
- **关窗口 = 收进托盘**（可关）。真要退出走托盘菜单的「退出」。
- **开机自启**（设置页里开关）。写的是
  `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`，不用管理员权限。

  > **注册表是唯一的事实来源，`settings.json` 里不存这一项。** 用户完全可能
  > 绕过我们去改 —— 任务管理器的「启动」页、msconfig、各种清理工具都能删掉它。
  > 自己再记一份的话两边迟早对不上，设置页就会显示一个骗人的勾。所以每次现读。
  >
  > 写进去的是当前 exe 的**绝对路径**，所以挪了地方要重新勾一次。

> **踩过的坑：托盘那三个菜单项，被四件事轮流坑过一遍。**
>
> **一、「不在眼前」有两种。** 收进托盘是窗口被隐藏了；最小化时窗口
> **仍然是 visible 的**，只是缩进了任务栏。只看一个标志就会把另一种状态
> 判反 —— 左键点最小化的窗口会去「隐藏」一个已经看不见的窗口，等于没反应。
> 判据是 `IsWindowVisible && !IsIconic`。
>
> **二、窗口最小化时界面根本不跑。** 原来窗口的显隐是发
> `ViewportCommand::Minimized(false)` 让界面去做的。收进托盘时这条路走得通 ——
> eframe 对隐藏的窗口会走 `update_logic_only`，界面逻辑照常跑
> （`glow_integration.rs` 里 `show_ui == false` 那一支）。但**最小化时它连
> 这一轮都不跑**：没有 `WM_PAINT` 就没有 `RedrawRequested`，`logic()` 一次
> 都不会被调用，事件一直躺在通道里。
> → 窗口的显隐改由**托盘线程自己动手**（`tray::apply`，一条 `SW_RESTORE`
> 覆盖两种状态）。托盘有独立的消息循环，主窗口最小化不影响它。
> 界面那边只跟着同步记账。
>
> **三、「退出」发的 `Close` 会被「关窗口 = 收进托盘」拦下来**，点了只是又
> 藏一次。加 `quitting` 标记把两种 `Close` 分开；关闭处理和托盘事件一样放在
> `logic()` 里，而且 `pump_tray` 要排在 `pump_close` 前面 —— 托盘的「退出」
> 是靠一条 `WM_CLOSE` 把界面叫醒的，得先收到 `Quit` 把标记立起来。
>
> **四、设置窗口不能和「叫出主窗口」同一帧开。** 它是主窗口的 immediate
> 子视口，建的时候父窗口必须已经在屏幕上；早一帧的话 eframe 建不出子窗口就
> 跳过回调，egui 直接 `panic: the user callback was never called`，
> **整个程序当场退出** —— 用户看到的「点设置，软件没了」就是它。
> 所以一律等主窗口再画过一帧（`App::settings_after`）。
>
> 顺带修掉的第五件：托盘窗口本来挂在 `HWND_MESSAGE` 下，而 message-only
> 窗口**当不了前台窗口**，`TrackPopupMenu` 要求菜单的属主是前台 ——
> 菜单会点不中、点别处也不消失。改成不显示的顶层窗口（`WS_EX_TOOLWINDOW`，
> 不带 `WS_VISIBLE`）就正常了。
>
> 这九种组合现在**能一键重跑**：`pwsh tools/tray-matrix.ps1` 起真的程序、
> 摆三种窗口状态、投三个菜单项，逐格报「目标反应 / 实际反应」。
> 纯决策部分另有单元测试（`tray.rs` 里那张表）。

- **设置是独立的子窗口**（`show_viewport_immediate`），托盘菜单进，
  Esc 或关窗口退出。能调语速、载体句开关、唤出快捷键、配色、托盘行为。
  设置存在工程根的 `settings.json`，坏了就退回默认值而不是打不开词典。

  用 immediate 而不是 deferred：deferred 的回调要求 `Fn + Send + Sync + 'static`，
  就得把 `Tts`、`Tray` 这些状态搬进 `Arc<Mutex>`；immediate 的回调能直接借
  `&mut self`。**代价是子窗口由主窗口那一帧驱动** —— 主窗口藏起来时 egui 不出帧，
  子窗口也就不刷新，所以打开设置时会连主窗口一起叫出来（但焦点给设置窗口，
  否则 Esc 会跑到主窗口去）。

### 音色

模型是 **Kokoro v1.1-zh**（`hexgrad/Kokoro-82M-v1.1-zh`），103 个音色**按语言分段**
（[sherpa-onnx PR #1942](https://github.com/k2-fsa/sherpa-onnx/pull/1942)）：
0–2 英文女声（`af_maple` / `af_sol` / `bf_vale`）、3–57 中文女声、58–102 中文男声。

**中文和英文都固定用 0 号（`af_maple`），不给选** ——
`dict_tts::VOICE`，代码里只有这一个常量，配置和设置页都没有对应项。

理由是听出来的，不是推出来的。挑音色时把这些都合成同一句样本听过一轮：

| 听过的 | 结论 |
|---|---|
| Kokoro 自带的 3–102 号中文音色 | 吐字普遍含混；同一句要念 7.9 秒，别家约 4 秒 |
| matcha-icefall-zh-baker（flow matching）| 没有好到值得多养一个模型 |
| piper huayan / xiao_ya / chaowen | 同上 |
| vits-melo-tts-zh_en、vits-zh-ll | 同上 |
| vits-icefall-zh-aishell3 | **8 kHz**，电话带宽，硬上限 |
| vits-zh-hf-theresa / eula（游戏角色语音）| 音色有特点，清晰度不占优 |

**0 号念中文是带口音的，但吐字比上面任何一个都清楚。** 查词要的是听清是哪个字，
不是听着像母语者 —— 这条判据一立，选型就没有悬念了。

所以 `models/` 下只留 Kokoro 一个模型，上面那些全部删掉了；
`--sweep` 那套批量出样本的代码也一并撤了（挑完就没用了，留着是死代码）。
真要再挑一次，从 git 历史里捞回来比留在树上强。

> **踩过的坑**：中文音色的默认值一开始给了 0 —— 当时 0 是英文女声，
> 等于一直用英文音色念中文。后来改成中文段首、给了分段滑块，
> 再后来听下来发现**最早那个「错误」的配置反而最清楚**，于是索性定死。
> 记这一笔是因为：中间那版「修好了」的分段滑块，实际效果比 bug 还差。

**官方没有为这 103 个音色公布任何评分。** 网上常见的那张 Kokoro A/B/C/D 评分表是
**v1.0** 的，而 v1.1-zh 恰恰把 v1.0 的大部分音色砍掉了（模型卡原话 "drops many
voices"）。所以只能自己听，上面那一轮就是这么来的。

### 图标

程序内画出来，不引入 `.ico` 资源：一份 RGBA 同时喂窗口和托盘。
玫瑰底（声调一的颜色）+ 米色横线，16 像素下要认得出，所以只有块面没有细节。

**三个地方要用图标，来源各不相同，缺一个就少一处**：

| 位置 | 来源 |
|---|---|
| 标题栏 / Alt+Tab | 运行期发 `WM_SETICON` |
| 系统托盘 | `Shell_NotifyIcon` 带的 HICON |
| **桌面快捷方式 / 资源管理器 / 任务栏** | **exe 里嵌入的图标资源** |

> **踩过的坑，两个。**
> 一、色位图必须走 `CreateDIBSection` 而不是 `CreateBitmap` —— 后者建的是
> **设备相关位图**，做图标时 alpha 会被丢掉，托盘里就是一块空白。
> 二、只做前两者是不够的：快捷方式和资源管理器**根本不看运行期的窗口图标**，
> 它们读的是 exe 的资源段。所以 `build.rs` 现画一张多尺寸 .ico
> （16/20/24/32/48/64/128/256），用 MinGW 的 `windres` 编成 COFF 链进去 ——
> 不往版本库里塞二进制资源。找不到 `windres` 时只警告不失败。

改了图标之后资源管理器可能还显示旧的，那是它自己的缓存：
重建一次快捷方式，或跑 `ie4uinit.exe -show`。

**滚动条一律不画**（`ScrollBarVisibility::AlwaysHidden`），滚轮照常能滚 ——
长词条仍然看得全，界面上不多那根条。

> 顺带记一笔：`ScrollArea` 默认 `auto_shrink` 会**收缩到内容宽度**，滚动条就跟着
> 贴在内容右边 —— 看起来就是「滚动条跑到页面中间」。三处滚动区都显式关掉了。

**已知取舍**：候选和词条不能同时看，查近义词辨析（将就/迁就）需要来回切。
如果实际用着碍事，考虑在词条页顶部加一行极窄的横向候选条（只有词形无释义）。

### 自截图

这台机器上外部截图抓不到本窗口 —— `CopyFromScreen` 只拿到壁纸，`PrintWindow`
能拿到边框但拿不到 OpenGL 客户区。所以做了自截图：

```bash
DICT_SHOT=shots ./target/release/dict.exe
```

程序会按 `crates/dict-app/src/shot.rs` 里的脚本走一遍各个状态，每步存一张
BMP 后退出。调 UI 时一直有用。

脚本后几步是**把事件喂进 egui 的输入队列**，走真正的交互路径：
第 9 步逐字符敲字（验证 `resp.changed()` 和逐击键的稳定排序），
第 10–12 步走 ↓↓ → 回车 → ↓ （验证「退回候选并保留原选中项」这条返回路径）。
前面几步直接改 `query` 字段，这些是验证不到的。

---

## 释义质量

CC-CEDICT 的英文释义是社区写的，义项不分、词性缺失、语域标注空白。
这是它和付费词典的**真实差距，也是工程量最大的地方 —— 不是索引能解决的**。

### 先量再动：问题比想象的小

```
CC-CEDICT 义项数分布
  1 个义项   79,849 条   63.9%   ← 没什么可重组的
  2 个义项   28,439 条   22.8%   ← 基本也不需要
  3 个及以上 16,680 条   13.4%   ← 「义项不分」真正发作的地方
```

六成词条只有一个义项。原设计说「跑前 2–3 万条」，但让模型碰单义项词条只会
增加幻觉风险，没有任何收益。加上频率筛选（百分位 ≥ 0.55），**目标约 1 万条**（实际导出 10,669 条）。

### 构建期做的（纯规则，不用模型）

- **先滤掉退化义项**：`also pr. [hua1]`、`Taiwan pr. [bi3]` 这类条目唯一的内容
  就在注音方括号里，剥掉就是空壳，留在释义里纯属噪音（全库 685 条）。
  判据是「剥完还剩不剩东西」而不是「开头像不像注记」—— `variant of 裏|里[li3]`
  剥完还剩「里」，是有效交叉引用，不能一起滤掉。
  整条都是空壳时原样保留：丢词条会让后面所有 id 平移，而旁路表按 id 索引。
- CC-CEDICT 的 `/` 分隔即义项，逐条拆开
- 开头的括号标记提成语域字段（`(coll.)` `(lit.)` `(fig.)` …）
- 交叉引用 `繁|简[pin1 yin1]` 摘成可点的「对应词条」，正文清成人能读的形式
- `CL:` 量词提成单独一行，不占义项号
- 词性来自 jieba
- 英文侧：ECDICT 的 `translation` 逐行成义项并切出词性前缀，行数对得上时
  用 `definition` 逐条配成补充说明（对不上就不硬配，免得张冠李戴）

### 义项重组（用 Claude，`dict-build llm`）

```bash
dict-build llm export                    # 选目标词条 → data/llm-work.jsonl
dict-build llm run --jobs 8 --batch 20   # 调 claude 重组，可断点续跑
dict-build llm import                    # 校验后合进旁路表
```

**角色是「重组」不是「生成」。** 模型只看得到 CC-CEDICT 已有的义项和挂好的
Tatoeba 例句，做的是排序、合并、加语域和搭配标注。拼音、声调、词形、词频
一律不进出模型 —— 声调尤其危险，模型编得极像真的。

**和语音合成的关键不对称：** TTS 错了一秒钟就听出来，释义错了可能带着用好几年。
所以这里的防线不是信任，是**机器可校验的接地约束**：每个输出义项必须用 `from`
列出它来自哪几条输入义项，导入时逐条比对原始义项下标，越界或凭空多出来的
整条打回，回退原始释义。

**旁路表，不动主索引。** 结果落在 `llm.bin`/`llm.idx`，按词条 id 索引：
重跑 ETL 不会丢掉这份产出，换模型或改 prompt 也能单独重来。
运行期有就覆盖显示，没有就回退。**词条页的折叠区永远保留原始释义**，
重组过的会标成「义项已重组 · 展开对照原始 CC-CEDICT 释义」。

> **踩过的坑：自我毒化，而且犯了两次。** 重组流水线的每一步都要拿**原始**义项
> 做基准，可 `Store::entry()` 会套上上一轮的旁路表：
> - `import` 用它取义项数校验 `from` 下标 → 基准变小 → 合法结果被判越界（误杀 21%）
> - `export` 用它判断义项够不够多 → 义项变少 → 10,770 条候选缩水成 3,008 条
>
> 两次都是**静默**的，只有对着数字较真才看得出来。所以最后不是「记得用对函数」，
> 而是加了 `Store::open_raw()` 从入口断掉：构建期一律走它，根本看不到旁路表。
> `store.rs` 里有专门钉这两个 bug 的回归测试。

**已跑完一轮**（`claude -p --model sonnet`，8 路并发 × 每批 20 条）：

```
10,669 条送出 → 10,663 条模型给出结果 → 10,645 条通过接地校验并写入
              → 打回 18 条（from 越界为主，另有义项凭空变多的）
用时 71.7 分钟，约 0.4 秒/条
效果：90% 的词条完成义项合并；新增 4,256 处语域标注、1,477 处搭配
```

单次 CLI 往返有约 15 秒固定启动开销，所以必须大批量 + 多进程 ——
1 路每批 8 条是 7 秒/条，8 路每批 20 条才降到 0.4 秒/条。

效果示例（花：7 义项 → 4 组）：

```
原始  flower / blossom / fancy pattern / florid / to spend (money, time) / (coll.) lecherous / lustful
重组  flower; blossom
      to spend (money, time)          搭配 花钱 / 花时间
      fancy pattern; florid
      lecherous; lustful              coll.
```

**源义项覆盖 99.96%。** 第一轮跑完时是 96.4%（384 条丢了源义项），
根因是那些退化义项（`also pr.` 之类）本来就不该进模型。治本之后：
ETL 滤掉空壳 → 重跑受影响的 286 条 → 覆盖率 99.96%，
剩下 4 条在导入时被打回、回退原文，**实际显示的内容零信息损失**。

「全覆盖」现在是 import 的**硬约束**而不是 prompt 里的请求：
空壳已经滤净，「丢掉一条源义项」再没有正当理由，漏了就整条打回。

**还没做**：常见误用、近义词辨析（将就/迁就那种）。那部分是纯生成，风险最高，
拿不出 `from` 这样的接地凭据，需要另设校验方式。英文侧 ECDICT 同样乱
（「编译, 编辑, 编纂, 收集」），结构好一档，可以照这套流程再跑一遍。

## 启动

窗口 **0.39 s** 出来（本机实测，热缓存）。慢的从来不是开窗口，是陪着它一起
启动的那些东西：

| 花在哪 | 耗时 |
|---|---|
| 打开词库（mmap + fst） | 15 ms |
| 建 GL 上下文、装字体、起托盘 | 约 350 ms |
| **语音引擎**（311 MB 模型 + 14 MB 词典 + 540 MB cuDNN DLL） | **约 2.7 s** |
| 索引预热（62 MB 顺序读） | 后台 |

后两项都挪到**第一帧画完之后**才放出去（`app::Deferred` / `App::start_deferred`）。
总耗时没变，但代价落在用户已经能打字的时间里 —— 而且不再和创建 GL 上下文
抢磁盘和 loader lock（有并发负载时实测把开窗口从 0.5 s 拖到 1.75 s）。

语音引擎 3.2 s 才就绪是模型本身的体量决定的，没有便宜可占：换 int8 模型会
换掉刚定下来的音色，`DICT_TTS_PROVIDER=cpu` 反而更慢（3.4 s，且省不掉模型加载）。
点播放键之前它早就好了。

> 如果某次启动明显更慢，先想想是不是刚重新编译过 —— Windows Defender 会把
> 新写出来的 exe 完整扫一遍，那一次是要多等几秒的。

## 发音

**机械味有两个不同来源，解法不同：**
- 模型太老（espeak-ng、Ekho 的共振峰/拼接合成）→ 换模型
- **更隐蔽**：神经 TTS 喂单个词。韵律模型在无上下文短文本上会退化成平调。
  这跟模型好坏无关，Kokoro 也一样。

**解法：载体句合成再裁剪。** 不合成「将就」，合成「这个词读作，将就」，
再按最后一处停顿把目标段切出来（逗号是为了逼出那个韵律停顿）。
切不出来就退回直接合成单词，而不是把整句载体念给用户听。
`carrier: false` 可以关掉它做 A/B。

**部署**：sherpa-onnx 预编译 DLL，dlopen 加载（`crates/dict-tts/src/ffi.rs`）。
模型是 `kokoro-multi-lang-v1_1`（Apache 2.0，中英同一个模型，固定 0 号音色）。
播放走 winmm 的 `PlaySoundW` + `SND_MEMORY`：一次调用，异步，新的播放自动
打断上一次 —— 正是词典要的行为。

### 本机实测 RTF

同一轮测量，8 线程，RTX 4060：

| | 词头（载体句） | 例句 | 结论 |
|---|---|---|---|
| CPU | 1441 ms / 2625 ms = **0.55** | 2419 ms / 4175 ms = **0.58** | 都得预热，否则点一下要干等 |
| GPU | 140 ms / 2625 ms = **0.053** | 476 ms / 4175 ms = **0.114** | 点了就合成也来得及 |

> 早先记的 CPU 数字是 0.34，那是另一轮测量。RTF 随机器负载浮动得厉害，
> 上面这组是同一轮里测的，横向可比。**要点不是具体数值，是量级** ——
> CPU 上一次合成以秒计，所以预热不是优化而是必需。

GPU 快约 10 倍。三样齐全（CUDA 版 sherpa 运行库 + cuDNN 9 + CUDA Toolkit）
才走 GPU，缺一件就用 CPU；即便选了 CUDA，引擎起不来时还会再自动回落一次。
`DICT_TTS_PROVIDER=cpu|cuda` 可以强制。启动时会打印实际用的是哪个。

**CUDA 依赖必须按绝对路径预加载**（`preload_cuda`）。光改 PATH 不行 ——
onnxruntime 用带 `LOAD_LIBRARY_SEARCH_*` 标志的方式加载 provider，那条路径
不搜 PATH，会报「depends on cudnn64_9.dll which is missing」。加载器的救命规则是
「同名模块已在进程里就直接复用」，所以自己按依赖顺序先 dlopen 一遍。
另外 cuDNN 和 CUDA Toolkit 的 DLL 都在 `bin/x64/` 而不是 `bin/`。

**打开词条页时会预热两样：词头，和第一条例句。**

例句原来既不缓存也不预取 —— 在 GPU 上无所谓（0.5 秒），但没有独显的机器
CPU 合成一句要两秒半，点一下干等两秒半是不能接受的。现在词头和例句走同一个
带缓存的入口（`Engine::samples`），键用 `Cue` 区分：**同一段文字作为词头和
作为例句是两份不同的波形**（前者走载体句裁剪），不能共用一个键。

预热在工作线程里排在播放之后，而且词头和例句**各留最后一条** ——
打开词条页会一次发两条，只留一条会把词头挤掉；快速翻词条时积压的旧请求
也不该再算，留最后的即可。

```bash
cargo run --release -p dict-tts --example say -- models/kokoro-multi-lang-v1_1 --cuda
cargo run --release -p dict-tts --example say -- models/kokoro-multi-lang-v1_1 --cuda --play
```

不加 `--play` 只测 RTF 不出声；加了才实际播放，并且同一批词分别用
「载体句裁剪」开/关各念一遍，方便用耳朵 A/B。

**选型不看排名，自己听** —— 覆盖单字、双字、四字成语、多音字、英文词。
网上那些「2026 最佳 TTS 榜单」MOS 数字来自模型作者自评，不构成选型依据。
这一轮已经听完并定死，见上面「音色」一节。

---

## 发布

打一个 `v*` 标签，`.github/workflows/release.yml` 就在 CI 上构建、打包、
建 GitHub Release 并挂上 zip。构建放在 CI 而不是本机：runner 自带 MSVC，
产出的是标准 msvc 二进制，比开发机上那份 windows-gnu 的更适合发给别人。

```powershell
pwsh tools/package.ps1 -Version v0.1.0    # 本地打一份看看
git tag v0.1.0 && git push origin v0.1.0  # 真发布
```

### 包里只有程序，没有词库

zip **4 MB**：`dict.exe` + `dict-build.exe` + `setup.bat` + `README.txt`。

词库数据一概不进包 —— 它们不属于本项目，各有各的授权，尤其 **ECDICT 授权
不明确**。把 400 MB 来路不清的派生数据挂到公开 Release 上，和「不碰商业词典
MDX」是同一条线。所以数据由用户跑一次 `setup.bat`，程序自己从各家官方地址取
（约 850 MB，之后建索引约 40 秒）。

### `dict-build fetch`

`tools/fetch-data.sh` 干的事，但**不需要 bash**：发布包里的用户只有一个
Windows，没有 Git Bash、没有 gzip、没有 bunzip2。

外部只用 Windows 自带的两个命令：`curl.exe`（免掉整套 HTTP + TLS 依赖）和
`tar.exe`（bsdtar，zip 和 tar.bz2 都认）。它俩办不了的两种用纯 Rust 解：

- `.gz` 单文件 → `flate2`（miniz_oxide 后端，不编 C）
- `.bz2` 单文件 → `bzip2-rs`

> **踩过的坑：bsdtar 读不了裸 bz2。** 它自己链着 bz2lib，但只认归档格式，
> 喂一个单文件 `.bz2` 直接报 `Unrecognized archive format`。
> 而 Tatoeba 的两个句子文件恰好就是裸 bz2 —— 这一条就是必须引入纯 Rust
> 解压器的全部原因。

### 没有词库时要弹窗

这是个 GUI 子系统的程序，**没有控制台**：启动失败时 `eprintln!` 谁也看不见，
双击之后就是「什么都没发生」—— 而第一次运行还没跑 `setup.bat` 的人，
见到的正是这一幕。所以 `main` 把错误接住，用 `MessageBoxW` 弹出来
（`tray::alert`）。

### 已知的事

- 只支持 Windows x64；没有代码签名，SmartScreen 会拦一下。
- **CI 出的 exe 不带图标资源**，快捷方式是白板：`build.rs` 靠 MinGW 的
  `windres` 嵌入 .ico，而 CI 上是 MSVC，没有这个工具。窗口和托盘图标不受影响，
  那两处是运行期画的。

---

## 工具链

**有 MSVC 的话这一节可以跳过** —— 直接 `cargo build`，下面这些坑都是
windows-gnu 这条路上独有的。

本机没有 MSVC build tools，也没有 Windows SDK，msvc 工具链根本链接不了，
所以走 `x86_64-pc-windows-gnu`，并且踩了三个坑。
`tools/setup-toolchain.sh` 把这三件事一次做完：

1. **rustup 自带的 mingw 是「仅用于链接」的精简版**（见它自己的
   `GCC-WARNING.txt`）：没有 `as.exe`，导入库只有 44 个，缺 `shlwapi`
   （eframe 依赖链要用）。→ 下一份完整的 MinGW-w64 到 `.bin/mingw64`
   （winlibs，**msvcrt 变体**，与 Rust 的 windows-gnu 目标匹配；ucrt 变体对不上），
   并 `-Clink-self-contained=no`，让 CRT 对象和 libgcc 全部由这一份提供。

2. **raw-dylib 需要 dlltool。** GNU dlltool 还要一个外部汇编器；
   `llvm-ar` 以 `llvm-dlltool` 的名字调用时是自包含的。→ `-Cdlltool=…`

3. **GNU ld 给 eframe 这种体量的映像链出来的二进制是坏的。**
   启动时 `__do_global_ctors` → `__gcc_register_frame` 跳到野地址，
   `0xC0000005`，**崩在 main 之前**。与 `--gc-sections`、ASLR、
   `panic=abort`、静态还是共享 libgcc 都无关；单独链接 egui 或单独链接 winit
   都不触发，合起来才会。→ `-Clink-arg=-fuse-ld=lld` 换 LLD。

   LLD 是这么放进去的：`rust-lld.exe` 本身就是 LLD 的 multicall 二进制，
   按 `argv[0]` 分发 —— **改名成 `ld.lld` 就是 GNU 兼容驱动**。把它放进
   `.bin/mingw64/x86_64-w64-mingw32/bin/`，那个目录本来就在 gcc 找辅助程序的
   搜索路径上（`gcc -print-search-dirs` 能看到），于是 `-fuse-ld=lld` 就够了，
   不必再写一条 `-B` 绝对路径。
   注意**不能直接拷 `gcc-ld/ld.lld.exe`**：那个是包装器，它按相对位置去找
   `rust-lld`，挪了地方就找不着（报 `error running rust-lld child process`）。

排查过程留个记录：崩在 main 之前时 stderr 是空的，先用「写文件」而不是
stderr 确认「真的崩在 main 之前」，再用 winlibs 自带的 gdb 拿栈回溯 ——
`__do_global_ctors` 这一帧是全部线索的来源。

### `.cargo/config.toml` 是生成的，不进版本库

模板是 `.cargo/config.toml.example`，`setup-toolchain.sh` 把里面的 `@ROOT@`
换成本机路径。**为什么非得生成**：里面必须是绝对路径 —— cargo 的 rustflags
不做变量展开，而 rustc 找 dlltool 走的是 PATH，config 里也没法往 PATH 前面插
目录。（`linker` 那一项其实能用相对路径，cargo 会按 config 文件所在目录解析；
但 `-Cdlltool` 在 rustflags 里，只能给绝对路径。）

同理**不用 `rust-toolchain.toml`**：它会盖过 `rustup override`，等于把有 MSVC
的人也强行按到 gnu 上。改成 setup 脚本里跑一条 `rustup override set`，
只影响本机。

### 零 C 依赖

没有 `cc` crate，没有 `-sys` crate 从源码编译。需要 C 库的两处都走 dlopen ——
构建期读 ECDICT 用 Windows 自带的 `winsqlite3.dll`，运行期发音用预编译的
sherpa-onnx DLL。`libloading` 也没用（它经 `windows-link` 走 raw-dylib，
还是要 dlltool），换成 `dict_core::dynlib` 里对 kernel32 的三个函数声明。

---

## CI

`.github/workflows/ci.yml`，跑在 `windows-latest` 上：

| 任务 | 干什么 |
|---|---|
| `test` | stable + beta 两条，`cargo build --all-targets` + `cargo test` |
| `lint` | `cargo fmt --check` + `cargo clippy -D warnings` |
| `release-build` | release 构建，产物传成 artifact |

两点值得记：

- **windows-latest 自带 MSVC，正好替我们跑 msvc 这条路。** 开发机上没有 MSVC，
  本地只验得了 windows-gnu —— msvc 能不能编能不能链，只有 CI 说了算。
- **release 单列一个任务。** GNU ld 那个「崩在 main 之前」的坑只在 release
  的体量上才发作，debug 构建过了不代表 release 过。
- **CI 出的 exe 不带图标资源。** `build.rs` 靠 MinGW 的 `windres` 把 .ico 编成
  COFF 链进去，而 MSVC 那边没有 windres —— 找不到时只警告不失败（图标是锦上
  添花，不该挡住构建）。所以从 CI artifact 下来的 exe，快捷方式会是白板；
  窗口和托盘图标不受影响，那两处是运行期画的。

CI 上**不下真实词库**（1.2 GB，不现实）。ETL 靠
`crates/dict-build/tests/fixture` 里那份**编出来的**微型词库跑通
（`crates/dict-build/tests/etl.rs`）：内容全是假的，不含任何真实词库的片段，
所以进版本库没有授权问题，却覆盖了真实数据里那几种要命的形态 ——
多音字（银行）、繁简异体（裡/里）、交叉引用、量词、语域标记、英文屈折。
`stardict.db` 是一个 20 KB 的 SQLite，字段和真库一致。

## 待办

### 值得先做
- [ ] 抽样人工核对重组结果（`data/llm-work.jsonl` 按频率降序，前几百条最值得看）。
- [ ] 英文侧 ECDICT 义项重组 —— 同一套流水线，改一下取词条件即可。
- [ ] 常见误用 / 近义词辨析：纯生成，需要另设接地校验，暂缓。

### 试过又撤掉的

**划词取词**（选中任意窗口的文字 → Alt+1 → 直接出词条）。写完测过又整个撤了，
撤的理由记在这里，免得以后重新纠结一遍：

- Windows 没有「读取任意窗口选中内容」的公开接口，通行做法是往前台窗口
  **注入 Ctrl+C 再读剪贴板**。三条规矩：必须赶在抢焦点之前做（否则复制的是
  自己的输入框）、前台是自己就不抓、用完把剪贴板还回去。
- **代价躲不开**：没选中东西时也得等剪贴板序号变化，而「没选中」和「目标程序
  不响应 Ctrl+C」分辨不出来。压到 150 ms 已经是极限，也就是说**每次按 Alt+1
  只想开个窗口，都要白等这 150 ms**。
- 还有个还不回去的东西：剪贴板里原来若是图片或文件，抓完就没了 ——
  只还得回文本，做到无损要接管全部剪贴板格式，代价远大于收益。

真要再做，**别挂在 Alt+1 上**：单独给一个热键，让唤出保持即时。

### 之后
- [ ] 词条页顶部的横向候选条（如果两态切换实际用着碍事）
- [ ] 明暗配色跟随系统实时切换（现在只在启动时取一次，F1 条里可手动切）

## 便利工具

```powershell
# 窗口行为回归：托盘菜单九宫格 + 标题栏那三个键，跑真程序看真反应
pwsh tools/tray-matrix.ps1

# 打一份发布包（先 cargo build --release --workspace）
pwsh tools/package.ps1 -Version v0.1.0
```

```bash
# 拉数据源，不需要 bash 也能跑（发布包里的用户走的就是这条）
cargo run --release -p dict-build -- fetch
```

```bash
# 命令行查词，用来核对打分行为，不进界面
cargo run --release -p dict-build --bin dict-query -- 事 china zhonggu man 编译
cargo run --release -p dict-build --bin dict-query          # 跑一组内置用例并报时延

# 核对义项重组：重组后的义项和 CC-CEDICT 原文并排打出来
cargo run --release -p dict-build --bin dict-query -- --raw 意思 花
```
